export interface Group {
  id: string;
  name: string;
  ownerUser: {
    fullname: string;
  };
}

export interface GroupDetail {
  id: string;
  name: string;
  ownerUser: {
    fullname: string;
  };
}
